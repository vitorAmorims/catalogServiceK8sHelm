apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "play-catalog.fullname" . }}-test-connection"
  labels:
    {{- include "play-catalog.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "play-catalog.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
