apiVersion: v1
kind: Service
metadata:
  name: {{ include "play-catalog.fullname" . }}
  labels:
    {{- include "play-catalog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
      {{- if and (eq .Values.service.type "NodePort") .Values.service.nodePort }}
      nodePort: {{ .Values.service.nodePort }}
      {{- end }}
  selector:
    {{- include "play-catalog.selectorLabels" . | nindent 4 }}
